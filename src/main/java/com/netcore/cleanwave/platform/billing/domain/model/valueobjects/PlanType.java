package com.netcore.cleanwave.platform.billing.domain.model.valueobjects;

/**
 * Value object enum representing the types of subscription plans available on the platform.
 */
public enum PlanType {
    FREE,
    PREMIUM,
    ANNUAL
}
