package com.netcore.cleanwave.platform.billing.domain.model.valueobjects;

/**
 * Value object enum representing the lifecycle status of a laundry subscription.
 */
public enum SubscriptionStatus {
    ACTIVE,
    CANCELLED,
    EXPIRED
}
