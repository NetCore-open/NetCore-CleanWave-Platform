package com.netcore.cleanwave.platform.logistics.domain.model.valueobjects;

/**
 * Value object enum representing the type of logistics operation (pickup or delivery).
 */
public enum DeliveryType {
    PICKUP,
    DELIVERY
}
